---
DIGIT_STRING		# LHS
# The first after LHS is always the head
DIGIT 
# The second and following ones are the non-heads
DIGIT_STRING : {AlignBottom(0), PositionEast(0), HeightRelationEqual(0)}
# The number in the brackets are the index to the parameter

---
DECIMAL_NUMER	
POINT
DIGIT_STRING : {AlignBottom(0), PositionWest(0), HeightRelationGreater(0)} # Left digit string
DIGIT_STRING : {AlignBottom(0), PositionEast(0), HeightRelationGreater(0), HeightRelationEqual(1)} # Right digit string